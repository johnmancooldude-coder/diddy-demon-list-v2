# DIDDY DEMON LIST V17.4 — NO DELETIONS

## Purpose
This is a preservation/fix build based directly on V17.3. Existing pages and features are retained.

## Fix
- Restores/makes the 🏆 Achievements navigation link explicit across all HTML pages.
- Keeps Changelog, News, Stats, Admin, Hall, Records, Players, Simulator, Time Machine, Battles, Challenges, Universe, and all other existing pages.
- Keeps the homepage Achievements dashboard card.

## CONFIG
This ZIP intentionally does NOT contain a `config.js` with blank credentials. Keep the working `config.js` already in your GitHub repository. Do not delete or replace it.

## Deploy
Copy the site files into the existing GitHub Pages repository while preserving `config.js`.
