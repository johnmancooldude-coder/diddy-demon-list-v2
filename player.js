const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const id=new URLSearchParams(location.search).get('id');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function boot(){const el=document.getElementById('playerPage');if(!sb){el.innerHTML='<div class="panel">Connect Supabase first.</div>';return}const q=await Promise.all([sb.from('players').select('*').eq('id',id).single(),sb.from('v17_player_stats').select('*').eq('id',id).single(),sb.from('records').select('*,levels(*)').eq('player_id',id).order('created_at',{ascending:false}),sb.from('point_values').select('*')]);const bad=q.find(x=>x.error);if(bad?.error){el.innerHTML=`<div class="panel error">${esc(bad.error.message)}</div>`;return}const p=q[0].data,s=q[1].data,R=q[2].data||[],PV=q[3].data||[];if(!p){el.innerHTML='<div class="panel">Player not found.</div>';return}const pts=r=>Number(PV.find(x=>x.rank===r)?.points||0);const top=R.slice().sort((a,b)=>a.levels.rank-b.levels.rank);const xp=Math.round(Number(s.total_points)*1.2+Number(s.victories)*25),lvl=Math.floor(xp/100)+1,power=Math.round(Number(s.total_points)*2.4+Number(s.top10_victories)*22+Number(s.top25_victories)*8+Number(s.victories)*5);el.innerHTML=`<section class="hero"><div><p class="eyebrow">PLAYER PROFILE 3.0</p><h1>👤 ${esc(p.name)}</h1><p>Level ${lvl} · ${xp.toLocaleString()} XP</p></div></section><section class="adminQuick"><div><b>${Number(s.total_points).toLocaleString()}</b><span>points</span></div><div><b>${s.victories}</b><span>victories</span></div><div><b>#${s.highest_victory??'—'}</b><span>hardest win</span></div><div><b>${power}</b><span>power</span></div></section><section class="panel"><h2>📊 Career Analytics</h2><div class="twoCols"><div><p>Average placement: <b>${s.average_placement??'—'}</b></p><p>Top 10 wins: <b>${s.top10_victories}</b></p><p>Top 25 wins: <b>${s.top25_victories}</b></p><p>Top 50 wins: <b>${s.top50_victories}</b></p></div><div><p>Total attempts: <b>${Number(s.total_attempts||0).toLocaleString()}</b></p><p>Avg completion: <b>${s.avg_completion_seconds??'—'}s</b></p><p>First victory: <b>${s.first_victory_at?new Date(s.first_victory_at).toLocaleDateString():'—'}</b></p><p>Latest victory: <b>${s.latest_victory_at?new Date(s.latest_victory_at).toLocaleDateString():'—'}</b></p></div></div></section><section class="panel"><h2>🏆 Victory Timeline</h2><div class="list">${top.map((r,i)=>`<div class="adminRow row between"><span><b>#${r.levels.rank} ${esc(r.levels.name)}</b><span class="meta"> · ${new Date(r.created_at).toLocaleDateString()}</span></span><strong>${pts(r.levels.rank)} pts</strong></div>`).join('')||'<p class="meta">No victories yet.</p>'}</div></section><section class="panel"><h2>🎖️ Trophy Case</h2><div class="twoCols"><div class="trophy earned"><span>👑</span><b>Hardest Win</b><small>${s.highest_victory?`#${s.highest_victory}`:'Locked'}</small></div><div class="trophy ${s.top10_victories?'earned':''}"><span>🔥</span><b>Top 10 Hunter</b><small>${s.top10_victories||0} wins</small></div></div></section>`}boot();