const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function boot(){if(!sb)return;const {data,error}=await sb.from('v17_number_one_hall').select('*').order('recorded_at');if(error)return $('hofList').innerHTML=`<div class="panel error">${esc(error.message)}</div>`;const rows=data||[];$('hofStats').innerHTML=`<div class="listSummary"><span><b>${rows.length}</b> recorded #1 events</span><span>Every event is permanent history.</span></div>`;let cards=[];for(let i=0;i<rows.length;i++){const x=rows[i],next=rows[i+1],days=next?Math.max(0,(new Date(next.recorded_at)-new Date(x.recorded_at))/86400000):null;cards.push(`<div class="adminRow row between"><span><b>👑 #1 · ${esc(x.level_name)}</b><span class="meta"> · ${new Date(x.recorded_at).toLocaleString()}</span></span><strong>${days===null?'Current reign':days.toFixed(1)+' days'}</strong></div>`)}$('hofList').innerHTML=cards.join('')||'<p class="meta">No #1 history recorded yet.</p>'}boot();