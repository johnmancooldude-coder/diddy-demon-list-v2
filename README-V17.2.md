# DIDDY DEMON LIST V17.2 — CHANGELOG RESTORE

## Fixes
- Restores a visible **Changelog** link in the navigation across the site.
- Changelog page remains backed by the `public.changelog` table.
- News Desk announcements continue to publish into `changelog`, so announcements can appear in both News and Changelog.
- Corrects the About page Legacy section label to #101+.

## Deploy
Keep your existing `config.js`. Replace the site files with this build. No new SQL is required if V17.1 content SQL has already succeeded.
