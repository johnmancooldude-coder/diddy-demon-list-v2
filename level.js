const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const id=new URLSearchParams(location.search).get('id');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function boot(){const el=document.getElementById('levelPage');if(!sb)return el.innerHTML='<div class="panel">Configure Supabase first.</div>';const q=await Promise.all([sb.from('levels').select('*').eq('id',id).single(),sb.from('v17_level_stats').select('*').eq('id',id).single(),sb.from('records').select('*,players(name)').eq('level_id',id).order('created_at',{ascending:false}),sb.from('placement_history').select('*').eq('level_id',id).order('recorded_at',{ascending:false})]);const bad=q.find(x=>x.error);if(bad?.error)return el.innerHTML=`<div class="panel error">${esc(bad.error.message)}</div>`;const l=q[0].data,s=q[1].data,R=q[2].data||[],H=q[3].data||[];if(!l)return el.innerHTML='<div class="panel">Level not found.</div>';const currentRank=Number(l.rank);const sectionLabel=l.section==='main'?'MAIN':l.section==='extended'?'EXTENDED':'LEGACY';const recent=H[0],prev=H[1];const delta=recent&&prev?Number(prev.rank)-Number(recent.rank):0;const movement=delta>0?`<span class="rankUp">▲ ${delta}</span>`:delta<0?`<span class="rankDown">▼ ${Math.abs(delta)}</span>`:'';const thumbs=l.thumbnail_url?`<img class="levelHeroThumb" src="${esc(l.thumbnail_url)}" alt="">`:'';el.innerHTML=`<section class="hero levelHero3"><div>${thumbs}<p class="eyebrow">LEVEL PAGE 3.0 · ${sectionLabel}</p><h1>#${currentRank} · ${esc(l.name)} ${movement}</h1><p>${Number(s.points||0)} points · ${esc(l.status||'')}</p></div></section><section class="adminQuick"><div><b>${s.victor_count}</b><span>victors</span></div><div><b>${Number(s.unique_players||0)}</b><span>unique players</span></div><div><b>${Number(s.total_attempts||0).toLocaleString()}</b><span>attempts</span></div><div><b>${s.fastest_completion_seconds??'—'}${s.fastest_completion_seconds!=null?'s':''}</b><span>fastest run</span></div></section><section class="panel"><h2>📊 Level Statistics 3.0</h2><div class="statGrid"><div><span>Creator</span><b>${esc(l.creator||'—')}</b></div><div><span>Verifier</span><b>${esc(l.verifier||'—')}</b></div><div><span>Holder</span><b>${esc(l.holder||'—')}</b></div><div><span>Difficulty</span><b>${esc(l.difficulty||'—')}</b></div><div><span>Average completion</span><b>${s.avg_completion_seconds??'—'}${s.avg_completion_seconds!=null?'s':''}</b></div><div><span>Times ranked</span><b>${H.length}</b></div><div><span>First victory</span><b>${s.first_victory_at?new Date(s.first_victory_at).toLocaleDateString():'—'}</b></div><div><span>Latest victory</span><b>${s.latest_victory_at?new Date(s.latest_victory_at).toLocaleDateString():'—'}</b></div></div><p>${esc(l.description||'')}</p>${l.video_url?`<p><a class="button secondary" href="${esc(l.video_url)}" target="_blank" rel="noopener">▶ Verification video</a></p>`:''}</section><section class="panel"><h2>🏆 Victors</h2><div class="list">${R.map(r=>`<div class="adminRow row between"><span><a href="player.html?id=${r.player_id}"><b>${esc(r.players?.name||'Player')}</b></a><span class="meta"> · ${new Date(r.created_at).toLocaleDateString()}</span></span><span>${r.attempts?Number(r.attempts).toLocaleString()+' attempts':''}${r.completion_seconds?` · ${Number(r.completion_seconds).toFixed(1)}s`:''}${r.video_url?` · <a href="${esc(r.video_url)}" target="_blank" rel="noopener">video</a>`:''}</span></div>`).join('')||'<p class="meta">No victories yet.</p>'}</div></section><section class="panel"><h2>📈 Placement History</h2><div class="list">${H.map((x,i)=>`<div class="adminRow row between"><span>#${x.rank} · ${new Date(x.recorded_at).toLocaleString()}</span><span class="meta">${i===0?'Current recorded position · ':''}${esc(x.note||'')}</span></div>`).join('')||'<p class="meta">No recorded history.</p>'}</div></section>`}boot();