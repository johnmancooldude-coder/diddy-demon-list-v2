const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const id=new URLSearchParams(location.search).get('id');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function boot(){const el=document.getElementById('levelPage');if(!sb)return el.innerHTML='<div class="panel">Configure Supabase first.</div>';const q=await Promise.all([sb.from('levels').select('*').eq('id',id).single(),sb.from('v17_level_stats').select('*').eq('id',id).single(),sb.from('records').select('*,players(name)').eq('level_id',id).order('created_at',{ascending:false}),sb.from('placement_history').select('*').eq('level_id',id).order('recorded_at',{ascending:false})]);const bad=q.find(x=>x.error);if(bad?.error)return el.innerHTML=`<div class="panel error">${esc(bad.error.message)}</div>`;const l=q[0].data,s=q[1].data,R=q[2].data||[],H=q[3].data||[];if(!l)return el.innerHTML='<div class="panel">Level not found.</div>';el.innerHTML=`<section class="hero"><div><p class="eyebrow">LEVEL PAGE 2.0</p><h1>#${l.rank} · ${esc(l.name)}</h1><p>${esc(l.section)} · ${Number(s.points||0)} points</p></div></section><section class="adminQuick"><div><b>${s.victor_count}</b><span>victors</span></div><div><b>${s.total_attempts||0}</b><span>attempts</span></div><div><b>${s.fastest_completion_seconds??'—'}s</b><span>fastest run</span></div><div><b>${H.length}</b><span>history events</span></div></section><section class="panel"><h2>📊 Level Statistics 2.0</h2><div class="twoCols"><div><p>Creator: <b>${esc(l.creator||'—')}</b></p><p>Verifier: <b>${esc(l.verifier||'—')}</b></p><p>Holder: <b>${esc(l.holder||'—')}</b></p><p>Difficulty: <b>${esc(l.difficulty||'—')}</b></p></div><div><p>First victory: <b>${s.first_victory_at?new Date(s.first_victory_at).toLocaleDateString():'—'}</b></p><p>Latest victory: <b>${s.latest_victory_at?new Date(s.latest_victory_at).toLocaleDateString():'—'}</b></p><p>Average completion: <b>${s.avg_completion_seconds??'—'}s</b></p><p>Added: <b>${new Date(l.created_at).toLocaleDateString()}</b></p></div></div><p>${esc(l.description||'')}</p></section><section class="panel"><h2>🏆 Victors</h2><div class="list">${R.map(r=>`<div class="adminRow row between"><span><a href="player.html?id=${r.player_id}"><b>${esc(r.players?.name||'Player')}</b></a><span class="meta"> · ${new Date(r.created_at).toLocaleDateString()}</span></span><span>${r.attempts?Number(r.attempts).toLocaleString()+' attempts':''}${r.video_url?` · <a href="${esc(r.video_url)}" target="_blank" rel="noopener">video</a>`:''}</span></div>`).join('')||'<p class="meta">No victories yet.</p>'}</div></section><section class="panel"><h2>📈 Placement History</h2><div class="list">${H.map(x=>`<div class="adminRow row between"><span>#${x.rank} · ${new Date(x.recorded_at).toLocaleString()}</span><span class="meta">${esc(x.note||'')}</span></div>`).join('')||'<p class="meta">No recorded history.</p>'}</div></section>`}boot();