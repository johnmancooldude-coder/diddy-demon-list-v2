const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;
async function boot(){const el=document.getElementById('players');if(!sb){el.innerHTML='<div class="panel">Connect Supabase to see live players.</div>';return}const {data,error}=await sb.from('player_leaderboard').select('*').order('total_points',{ascending:false});if(error){el.innerHTML='<div class="panel">Run the SQL setup first.</div>';return}el.innerHTML=(data||[]).map((x,i)=>`<article class="levelCard"><div class="thumb">#${i+1}</div><div><a class="levelTitle" href="player.html?id=${x.id}">${x.name}</a><div class="meta">${x.victors} victors · highest #${x.highest_victory??'—'} · avg ${x.average_placement??'—'}</div></div><div class="points">${x.total_points}</div></article>`).join('')||'<div class="panel">No players yet.</div>'}boot();
