const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function boot(){const el=document.getElementById('players');if(!sb){el.innerHTML='<div class="panel">Connect Supabase to see live players.</div>';return}const q=await Promise.all([sb.from('player_leaderboard').select('*').order('total_points',{ascending:false}),sb.from('records').select('*'),sb.from('levels').select('id,rank,name')]);if(q[0].error){el.innerHTML=`<div class="panel error">${esc(q[0].error.message)}</div>`;return}const data=q[0].data||[],records=q[1].data||[],levels=q[2].data||[],map=Object.fromEntries(levels.map(x=>[x.id,x])),max=Math.max(...data.map(x=>Number(x.total_points)||0),1);
 el.innerHTML=`<div class="listSummary"><span><b>${data.length}</b> players ranked</span><span>Points update automatically from victories</span><span><a href="analytics.html">Open Advanced Analytics →</a></span></div>`+(data.map((x,i)=>{const wins=records.filter(r=>r.player_id===x.id),xp=Math.round(Number(x.total_points||0)*1.2+wins.length*25),lvl=Math.floor(xp/100)+1,power=Math.round(Number(x.total_points||0)*3+wins.filter(r=>(map[r.level_id]?.rank||999)<=10).length*18+wins.filter(r=>(map[r.level_id]?.rank||999)<=25).length*7+wins.length*4),best=x.highest_victory?`#${x.highest_victory}`:'—',pct=Math.max(4,Math.round((Number(x.total_points)||0)/max*100)),last=wins.map(r=>map[r.level_id]).filter(Boolean).sort((a,b)=>a.rank-b.rank)[0];return `<a class="playerRankCard" href="player.html?id=${x.id}"><div class="playerRank">#${i+1}</div><div class="playerAvatar">${esc((x.name||'?').slice(0,2).toUpperCase())}</div><div class="playerMain"><strong>${esc(x.name)}</strong><div class="meta">${wins.length} victories · best ${best} · avg ${x.average_placement??'—'}</div><div class="rankMeter"><span style="width:${pct}%"></span></div>${last?`<small class="meta">Top victory: #${last.rank} ${esc(last.name)}</small>`:''}<div class="playerBadges"><span class="tag">⚡ ${power} POWER</span><span class="tag xpTag">LV ${lvl} · ${xp} XP</span><span class="tag momentumTag">${wins.filter(r=>Date.now()-new Date(r.created_at).getTime()<30*86400000).length>=3?'🔥 SURGING':wins.length?'⚡ ACTIVE':'🧊 QUIET'}</span></div></div><div class="playerPoints"><strong>${Number(x.total_points||0).toLocaleString()}</strong><small>points</small></div></a>`}).join('')||'<div class="panel emptyState"><div>👤</div><h2>No players yet</h2><p>Add players from Admin.</p></div>');}boot();
