const c260=window.DIDDY_CONFIG||{},sb260=c260.SUPABASE_URL&&c260.SUPABASE_ANON_KEY?supabase.createClient(c260.SUPABASE_URL,c260.SUPABASE_ANON_KEY):null,E260=DIDDY_V260.esc;
async function boot260Player(){const id=new URLSearchParams(location.search).get('id'),host=document.getElementById('playerPage');if(!sb260||!id||!host)return;const q=await Promise.all([sb260.from('records').select('*').eq('player_id',id).order('created_at'),sb260.from('levels').select('*'),sb260.from('placement_history').select('*').order('recorded_at'),sb260.from('player_leaderboard').select('*').order('total_points',{ascending:false})]);if(q.some(x=>x.error))return;const [R,L,H,LB]=q.map(x=>x.data||[]),wins=DIDDY_V260.buildWins(R,L,H),official=LB.findIndex(x=>x.id===id)+1,ranks=wins.map(x=>x.hRank),avg=ranks.length?ranks.reduce((a,b)=>a+b,0)/ranks.length:null,med=DIDDY_V260.median(ranks),top1=ranks.filter(r=>r===1).length,top3=DIDDY_V260.top3(wins),top10=DIDDY_V260.top10(wins),lowest=ranks.length?Math.max(...ranks):null,points=wins.reduce((s,x)=>s+x.historicalPoints,0),avgGap=wins.length>1?(wins.slice(1).reduce((s,x,i)=>s+(new Date(x.created_at)-new Date(wins[i].created_at))/864e5,0)/(wins.length-1)):null,maxPoints=wins.length?Math.max(...wins.map(x=>x.historicalPoints)):0,climb=DIDDY_V260.histRankEvents(id,R,L,H),gaps=DIDDY_V260.biggestGap(wins),st=DIDDY_V260.streak(wins),days=Date.now()-new Date(wins[0]?.created_at||Date.now()).getTime();const box=document.createElement('section');box.className='panel v260Stats';box.innerHTML=`<div class="panelHead"><div><p class="eyebrow">📊 PLAYER STATISTICS 3.0</p><h2>Career Stats</h2><p class="meta">Deeper performance metrics calculated from recorded victories and historical level ranks.</p></div><span class="tag">LIVE</span></div><div class="v260StatGrid"><div><b>${wins.length}</b><span>Victories</span></div><div><b>${top1}</b><span>#1 victories</span></div><div><b>${top3}</b><span>Top 3 victories</span></div><div><b>${top10}</b><span>Top 10 victories</span></div><div><b>${avg?avg.toFixed(1):'—'}</b><span>Average rank</span></div><div><b>${med?med.toFixed(1):'—'}</b><span>Median rank</span></div><div><b>#${lowest||'—'}</b><span>Lowest victory</span></div><div><b>${maxPoints.toLocaleString()}</b><span>Best points from one win</span></div><div><b>${climb.largestClimb?'+ '+climb.largestClimb:'—'}</b><span>Biggest climb</span></div><div><b>${climb.largestFall?'− '+climb.largestFall:'—'}</b><span>Biggest fall</span></div><div><b>${gaps?gaps+'d':'—'}</b><span>Longest gap</span></div><div><b>${st.current}</b><span>Current win streak</span></div><div><b>${st.longest}</b><span>Longest win streak</span></div><div><b>${avgGap===null?'—':avgGap.toFixed(1)+'d'}</b><span>Avg. days between wins</span></div><div><b>${points.toLocaleString()}</b><span>Historical points</span></div></div><p class="meta">Win rate: N/A — DIDDY records completed victories, but does not store failed attempts, so a true success percentage cannot be calculated.</p></section>`;const timeline=document.querySelector('#playerPage .v250Timeline')?.parentElement;const firstGrid=document.querySelector('#playerPage .v250IntelGrid');if(firstGrid)firstGrid.insertAdjacentElement('beforebegin',box);else host.appendChild(box);const rh=document.createElement('section');rh.className='panel v260RankHistory';const ev=climb.events;rh.innerHTML=`<div class="panelHead"><div><p class="eyebrow">📈 RANK HISTORY 3.0</p><h2>Recorded Rank Journey</h2><p class="meta">Victory-time rank reconstructed from placement history. ${ev.length} recorded points.</p></div><div class="row"><span class="tag">PEAK #${climb.peak||'—'}</span><span class="tag">CURRENT #${official||'—'}</span></div></div><div class="v260RankPath">${ev.map((x,i)=>`<div class="v260RankNode"><span>${i+1}</span><b>#${x.rank}</b><small>${DIDDY_V260.date(x.date)}</small><em>${E260(x.level)}</em>${x.delta===null?'':`<strong class="${x.delta>=0?'rankUp':'rankDown'}">${x.delta>=0?'▲ '+x.delta:'▼ '+Math.abs(x.delta)}</strong>`}</div>`).join('')||'<p class="meta">No victory history yet.</p>'}</div>`;if(timeline)timeline.parentElement.insertAdjacentElement('beforebegin',rh);else host.appendChild(rh)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot260Player);else boot260Player();
