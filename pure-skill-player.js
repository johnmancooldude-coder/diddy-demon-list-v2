(function(){const cfg=window.DIDDY_CONFIG||{};const sb2=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;function wait(){if(!sb2||!document.getElementById('playerPage'))return setTimeout(wait,250);load()}async function load(){const id=new URLSearchParams(location.search).get('id');if(!id)return;const [rq,pq]=await Promise.all([sb2.from('pure_skill_rankings').select('*').eq('player_id',id).maybeSingle(),sb2.from('player_leaderboard').select('*').eq('id',id).maybeSingle()]);if(rq.error||!rq.data)return;const official=window.__diddyOfficialRank||null;const box=document.createElement('section');box.className='panel pureSkillProfile';const delta=official?official-Number(rq.data.skill_rank):null;box.innerHTML=`<div class="row between"><div><p class="eyebrow">🥇 PURE SKILL</p><h2>#${rq.data.skill_rank} · ${String(rq.data.tier||'').replace(/</g,'&lt;')}</h2><p class="meta">Subjective skill ranking · official leaderboard is separate.</p>${rq.data.note?`<p>${String(rq.data.note).replace(/</g,'&lt;')}</p>`:''}</div><div class="profileBadge"><span>SKILL</span><b>#${rq.data.skill_rank}</b></div></div>${delta!==null?`<p class="meta">Official rank: #${official} · Skill vs official: ${delta>0?'▲ '+delta:delta<0?'▼ '+Math.abs(delta):'—'}</p>`:''}`;document.getElementById('playerPage').after(box)}wait()})();
