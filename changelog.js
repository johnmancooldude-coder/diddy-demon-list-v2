const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;async function boot(){const el=document.getElementById('changes');if(!sb){el.innerHTML='<div class="panel">Connect Supabase to see the live changelog.</div>';return}const {data,error}=await sb.from('changelog').select('*').order('created_at',{ascending:false});el.innerHTML=error?'<div class="panel">Create the changelog table using the SQL setup file.</div>':(data||[]).map(x=>`<article class="panel"><div class="eyebrow">${new Date(x.created_at).toLocaleString()}</div><h2>${x.title}</h2><p>${x.body}</p></article>`).join('')||'<div class="panel">No changes yet.</div>'}boot();
