const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
async function boot(){const el=document.getElementById('changes');if(!sb){el.innerHTML='<div class="panel">Connect Supabase to see the live changelog.</div>';return}const {data,error}=await sb.from('changelog').select('*').order('created_at',{ascending:false});if(error){el.innerHTML=`<div class="panel error">${esc(error.message)}</div>`;return}const rows=data||[];const release={title:'V2.0.3 — RANK HISTORY & MOVEMENT',body:'Added reliable 24-hour rank movement badges on level rows, records every level displaced by a move so ▲/▼ indicators appear consistently, tightened movement tracking for new level insertions, and improved the changelog so every release can clearly list what was added. Also added clearer movement tooltips.',created_at:'2026-09-03T00:00:00Z'};const hasRelease=rows.some(x=>String(x.title||'').toLowerCase().includes('v2.0.3'));const all=hasRelease?rows:[release,...rows];el.innerHTML=all.map((x,i)=>`<article class="changeCard"><div class="changeMarker">${i===0?'NEW':'↳'}</div><div><div class="eyebrow">${new Date(x.created_at).toLocaleString()}</div><h2>${esc(x.title)}</h2><p>${esc(x.body)}</p></div></article>`).join('')||'<div class="panel emptyState"><div>📜</div><h2>No changes yet</h2><p>The history starts when admins record an update.</p></div>'}boot();
