const cfg=window.DIDDY_CONFIG||{};const sb=cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let levels=[];
function boundary(sec){return sec==='main'?45:sec==='extended'?100:Infinity}
function sim(sec,rank,id){let main=levels.filter(x=>x.section==='main').sort((a,b)=>a.rank-b.rank);let ext=levels.filter(x=>x.section==='extended').sort((a,b)=>a.rank-b.rank);let leg=levels.filter(x=>x.section==='legacy').sort((a,b)=>a.rank-b.rank);for(const a of [main,ext,leg]){const i=a.findIndex(x=>x.id===id);if(i>=0)a.splice(i,1)};let arr=sec==='main'?main:sec==='extended'?ext:leg;let min=sec==='main'?1:sec==='extended'?46:101;let r=Math.max(min,Number(rank)||min);let idx=sec==='main'?r-1:sec==='extended'?r-46: r-101;idx=Math.max(0,Math.min(idx,arr.length));arr.splice(idx,0,levels.find(x=>x.id===id));main=main;ext=ext;leg=leg;while(main.length>45)ext.unshift(main.pop());ext.sort((a,b)=>a.rank-b.rank);leg.sort((a,b)=>a.rank-b.rank);main.forEach((x,i)=>x._new={section:'main',rank:i+1});ext.forEach((x,i)=>x._new={section:'extended',rank:46+i});leg.forEach((x,i)=>x._new={section:'legacy',rank:101+i});return [...main,...ext,...leg]}
function render(){const id=$('level').value;if(!id)return;$('section').value=$('section').value||'main';const sec=$('section').value;const rank=Number($('rank').value)||1;const after=sim(sec,rank,id),before=Object.fromEntries(levels.map(x=>[x.id,x]));const changed=after.filter(x=>before[x.id].section!==x._new.section||Number(before[x.id].rank)!==Number(x._new.rank));$('summary').innerHTML=`<div class="listSummary"><span><b>${changed.length}</b> levels change</span><span>Target: <b>${sec.toUpperCase()} #${rank}</b></span></div>`;$('previewOut').innerHTML=after.filter(x=>changed.some(c=>c.id===x.id)).slice(0,80).map(x=>{const b=before[x.id];return `<div class="adminRow row between"><span><b>${esc(x.name)}</b><span class="meta"> · ${b.section} #${b.rank}</span></span><strong>${x._new.section} #${x._new.rank}</strong></div>`}).join('')||'<p class="meta">No changes.</p>'}
async function boot(){if(!sb){$('summary').textContent='Configure Supabase first.';return}const {data,error}=await sb.from('levels').select('*').order('rank');if(error){$('summary').textContent=error.message;return}levels=data||[];$('level').innerHTML=levels.map(x=>`<option value="${x.id}">${x.section} #${x.rank} · ${esc(x.name)}</option>`).join('');$('level').onchange=render;$('section').onchange=render;$('rank').oninput=render;$('preview').onclick=render;$('apply').onclick=async()=>{const l=levels.find(x=>x.id===$('level').value);if(!l)return;if(!confirm(`Apply ${$('section').value} #${$('rank').value} to ${l.name}?`))return;const {error}=await sb.rpc('move_level',{p_level_id:l.id,p_new_section:$('section').value,p_new_rank:Number($('rank').value),p_name:l.name,p_creator:l.creator||'',p_verifier:l.verifier||'',p_holder:l.holder||'',p_description:l.description||'',p_video_url:l.video_url||null,p_thumbnail_url:l.thumbnail_url||null});if(error)return alert(error.message);alert('Rank change applied.');location.reload()};render()}boot();